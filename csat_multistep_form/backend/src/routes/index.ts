import healthRoutes from './health.route';
import surveySessionsRoutes from './survey-sessions.route';

/**
 * Route exports
 * 
 * All routes are now directly defined in route files instead of using OpenAPI-glue.
 */
export { healthRoutes, surveySessionsRoutes };
